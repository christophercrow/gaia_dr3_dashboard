services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: gaia
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
